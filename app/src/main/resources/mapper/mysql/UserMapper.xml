<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis3-mapper.dtd">

<!-- Mapperとxmlのマッピング -->
<mapper namespace=com.example.repository.UserMapper>
	<!-- マッピング定義(ユーザー) -->
	<insert id="insertOne">
		insert into m_user(
			userid
			,password
			,user_name
			,birthday
			,age
			,gender
			,department_id
			,role
		)
		
		value(
			#{userId}
			,#{password}
			,#{userName}
			,#{birthday}
			,#{age}
			,#{gender}
			,#{departmentId}
			,#[role}]
		)
	</insert>
</mapper>